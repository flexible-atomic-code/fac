
@SET_MAKE@

SHELL = /bin/sh

INSTALL = @INSTALL@

BINDIR = @prefix@/bin

TOPDIR = @TOPDIR@

CC = @CC@
CFLAGS = @CFLAGS@
CPPFLAGS = @CPPFLAGS@
LDFLAGS = @LDFLAGS@
LIBS = @LIBS@ @FLIBS@

ALL_CFLAGS = ${CPPFLAGS} ${CFLAGS} ${LDFLAGS} $ ${LIBS}

all: sfac scrm

sfac: 	sfac.c stoken.c ${TOPDIR}/libfac.a
	${CC} -o sfac sfac.c stoken.c ${ALL_CFLAGS}
scrm: 	scrm.c stoken.c ${TOPDIR}/libfac.a
	$(CC) -o scrm scrm.c stoken.c ${ALL_CFLAGS}

install:
	${INSTALL} -d sfac scrm ${BINDIR}

clean:
	rm -rf *.o *~ sfac scrm



